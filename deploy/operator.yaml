apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudhsm-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: cloudhsm-operator
  template:
    metadata:
      labels:
        name: cloudhsm-operator
    spec:
      serviceAccountName: cloudhsm-operator
      containers:
        - name: cloudhsm-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - cloudhsm-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "cloudhsm-operator"
